const person = {}
    person.firstName = "John"
    person.lastName = "Doe";
        console.log(person.lastName);

const person = {}
    person.firstName = "John",
    person.lastName = "Doe",
    person.country = "Norway"

        console.log(person.country);

const person = {
    name: "John", age: 50
};
console.log(person.name  +  " is "  + person.age)

const building = {};
    building.name = "Central tower",
    building.floor = 25
        console.log(building.name + " is " + building.floor);

// write different type:
const building = {
    name: "Central tower",
    floor: 25,
};
       